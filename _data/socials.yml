# Social links shown in the navbar and on the about page.
# Order here = order rendered. Comment out any line you don't yet have.

email: bozhang23@mails.tsinghua.edu.cn

# TODO: fill in after registering.
# scholar_userid:        # e.g. AAAAAAAAAAAA — your Google Scholar ID
# github_username:       # e.g. zhangbo

# WeChat QR is shown if the file exists. Drop a 240x240 PNG at
# assets/img/wechat-qr.png to enable it, then uncomment below.
# wechat_qr: wechat-qr.png

# Re-enable after dropping a 64x64 logo at assets/img/aicosmos-logo.png.
# (jekyll-socials cannot reliably fetch external favicons at build time.)
# custom_social:
#   logo: /assets/img/aicosmos-logo.png
#   title: AICosmos
#   url: https://aicosmos.ai

# CV PDF intentionally NOT exposed online — only the rendered web /cv/ page
# (driven by _data/cv.yml) is public. PDFs live locally in assets/pdf/ but
# are gitignored, so the live site has no formal-CV download link.
# cv_pdf: /assets/pdf/cv.pdf

rss_icon: false
