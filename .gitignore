_site
.bundle
.DS_store
.DS_Store
.idea
.jekyll-cache
.jekyll-metadata
.ruby-version
.sass-cache
.tweet-cache
.venv
assets/libs/
assets/rendercv/rendercv_output/*.typ
Gemfile.lock
node_modules/
vendor

# --- private / out-of-scope artifacts (not pushed to GitHub) ---
# Research source folders (paper PDFs and LaTeX — kept locally only)
# NOTE: leading slash anchors to repo root, so assets/img/aei4u/ etc. are
# NOT affected by these rules (macOS filesystem is case-insensitive).
/AEI4U/
/Awesome-CV/
/BAMI/
/aicosmos/
/embodiedact/
/syll/

# Other repo clones / experimental folders
/Micasa99.github.io/
/self-page/
/isolated_personal_page/

# Loose root-level files (originals already integrated into assets/)
/image.png
/image*.png
/image copy*.png
/me.jpg
/cv_en.pdf
/cv_zh.pdf
/cv_zh*.pdf
/个人介绍.md
/AICosmos_EN*.mp4
/Weixin Image_*.jpg
/texput.log

# Overlay folder — duplicate of root content; kept locally as source-of-truth
# but not needed in the published repo
personal-site/

# LaTeX build artifacts (compiled PDFs are in assets/pdf/; source kept in cv/)
cv/*.aux
cv/*.log
cv/*.out
cv/*.toc
cv/*.fdb_latexmk
cv/*.fls
cv/*.synctex.gz

# Compiled CV PDFs — kept locally only; not exposed on the live site.
# The web /cv/ page (rendered from _data/cv.yml) is the only public CV form.
assets/pdf/cv.pdf
assets/pdf/cv_zh.pdf
cv/*.pdf